# Generated in CI — never committed, since this repo is authored on Linux.
*.xcodeproj/
*.xcworkspace/
build/
DerivedData/
Payload/
*.ipa
.DS_Store

.claude/

# Stray debug captures
*.html

# Locally-staged simulator build
Prism-simulator.zip

# Downloaded artifacts
prism-sim/
prism-sim.zip

# Signing material. Never in this repo — it is public, and a leaked private key
# cannot be rotated when the certificate belongs to someone else's team.
*.p12
*.mobileprovision
*.cer
*.certSigningRequest
